// ATENÇÃO: Este arquivo contém a chave da API.
// Em um projeto real, esta chave NUNCA deve ser exposta no código do lado do cliente (frontend).
// O ideal é fazer as chamadas para a API a partir de um backend para manter a chave segura.
const chaveApi = "d3c6dfd3";

export { chaveApi };
